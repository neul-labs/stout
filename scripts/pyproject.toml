[project]
name = "brewx-sync"
version = "0.1.0"
description = "Sync script for brewx index"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.28.0",
    "zstandard>=0.21.0",
]

[tool.uv]
dev-dependencies = []
